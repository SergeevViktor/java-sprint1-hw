import java.util.Scanner;
    public class Converter() {
        double distance = (? * 0.75) / 1000; //1 шаг, переведено в километры
        double burnedKilocalories = (? * 50)/1000; //соженно килокалорий
    }

